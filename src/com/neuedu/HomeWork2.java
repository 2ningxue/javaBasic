package com.neuedu;

import javafx.scene.shape.Circle;

import java.util.Scanner;

public class HomeWork2 {
    public static void main(String[] args) {
         /*1 定义长方形类，含：
    特征：宽、高（整型）；
    方法：求周长、面积；
    方法3个：（1）无参——宽、高默认值为1；（2）1个参数——宽、高均为参数值；（3）2个参数——宽、高各为参数值。
    并对其进行测试*/
       /* HomeWorktest2 homework= new HomeWorktest2();
       homework.width=1;
        homework.height=1;
        System.out.println("无参");
        homework.oblong();
        homework.ovlong();
        System.out.println("有一个参数");
        homework.oblong2(5);
        homework.ovlong2(3);
        System.out.println("有两个参数");
        homework.oblong3(3,7);
        homework.ovlong3(6,8);*/


         /* 2 定义圆类，它有一个变量radius(半径)。从键盘输入数据，将半径传递给radius，编程计算并输出圆的周长和面积（确保输入的数据不为负数）。
    并对其进行测试*/
    /* Scanner input=new Scanner(System.in);
        System.out.println("请输入半径");
        int radius=input.nextInt();
        HomeWorktest2.oblong(radius);*/

         /* 3 定义一个计算器类，包括加、减、乘、除运算。
    并对其进行测试*/
  /*   Mathe  mathes=new Mathe();
      mathes.mathe(3,5);
*/
          /* 4 编写一个Java程序，并满足如下要求：
            1）编写一个Car类，具有：   特征：品牌(mark)——String类型   价格（price）、速度（speed）——int型
    功能：驾驶（void drive( )）    功能：变速（void speedChange(int newSpeed)），把新速度赋给speed
3）定义主类E，在其main方法中创建Car类的两个对象：aodi和benchi的对 象并测试其对象的特性。
*/
         /* HomeWorktest2  HomeWork2=new  HomeWorktest2();
        HomeWork2.setMark("aodi");*/

          /*5 定义一个学生类 包括 姓名 年龄 分数的特征
    创建五个学生对象 对其中特征进行赋值 赋值后 根据分数从大到小进行排序
*/
        /*HomeWork2[] stus=new HomeWork2[5];
for (int i=0;i<stus.length;i++){
    HomeWork2 student=new HomeWork2();
    HomeWork2.sname="abc";
    HomeWork2.score=60+i;
    stus[i]=HomeWork2;
}
for(HomeWorktest2 s:stus){
    System.out.println(s);
}
*/
    }
}
